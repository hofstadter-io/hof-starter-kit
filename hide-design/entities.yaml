app:
  name: "Hof Starter Kit"

  # Things which have identity and can take on roles
  # builtin: [user, group, org, service-account]
  entities:
    - name: user
      location: "modules/user"
      module: "dsl.modules.user.module.User"
      type: "type.modules.user.User"
