name: "default"
about: "DSL for types"
version: "0.0.1"
language: multiple
type: "type"

ignore-design-path: true

template-configs:

  - name: once-files
    field: "."


dependencies:

  designs:

    - name: once-files
      field: "."
      templates:
        - in: debug.txt
          out: "modules/{{name}}/module-debug.txt"
