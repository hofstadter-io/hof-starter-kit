name: "default"
about: "DSL for types"
version: "0.0.1"
language: multiple
type: "type"

ignore-design-path: true

template-configs:

  - name: once-files
    field: "."


static-files:

  - name: all
    field: "."
    unless: disabled
    files:
    - "**/*"

