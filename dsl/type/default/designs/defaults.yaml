{{#with DslContext as |TYPE| }}
{{#getdsl (concat2 "module." (replace TYPE.relPath "/" "." -1)) true}}{{#with . as |MODULE| }}
{{#getdsl "app" true}}{{#with . as |APP| }}

type:
  name: {{TYPE.name}}

  # Common
  {{> designs/defaults/auth.yaml }}

  # Client
  {{> designs/defaults/components.yaml }}
  {{> designs/defaults/pages.yaml }}

  # Server?

{{/with}}{{/getdsl}}
{{/with}}{{/getdsl}}
{{/with}}

