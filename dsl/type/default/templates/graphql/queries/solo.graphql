{{#with DslContext as |TYPE|}}
{{#with (camelT TYPE.name) as |TypeName|}}
{{#with (camel  TYPE.name) as |typeName|}}
#import "../fragments/type-solo.graphql"

#xxximport "./Comment.graphql"

query {{typeName}}($id: String!) {
  {{typeName}}({{typeName}}Id: $id) {
    {{typeName}}{
      ...{{TypeName}}Info
    }
  }
}
{{/with}}
{{/with}}
{{/with}}
